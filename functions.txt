Scene
synchronize
play
stop
pause
addItem
addElement
onAnimate
timerFunction
setPlaySpeed
addTimingFunction

Scene.SceneItem
setProperty
description : 해당 시간에 property를 설정한다.
arguments : time / Number , property / String, value / String | Number | Object
return : this / SceneItem:Object

getProperty
description : 해당 시간의 property를 가져온다.
arguments : time / Number , property / String
return : value / String | Number | Object

setFilter
getFilter
setTransform
getTransform


getNowFrame
해당 시간의 Frame을 구해서 가져온다.
arguments : time / Number
return : frame / Frame:Object

getPrevFrameByProperty
해당 property있는 frame 중 해당 time 보다 작고 근접한 frame을 가져온다.
aguments : time / Number, property / String
return: frame / Frame:Object

getNextFrameByProperty
해당 property있는 frame 중 해당 time 보다 작고 근접한 frame을 가져온다.
aguments : time / Number, property / String
return: frame / Frame:Object


onAnimate
재생되는 동안 불러지는 Callback 함수를 설정한다.
arguments : animateFunction / Function
return : this / SceneItem:Object

synchronize
해당 시간의 장면으로 전환한다.
arguments : time / Number
return: ??


addFrame
해당 time에 frame을 추가한다.
arguments: time / Number , frame / Frame:Object
return: this / SceneItem

copyFrame
해당 시간(fromTime)의 frame을 복사해 다음 시간(toTime)에 추가한다.
arguments: fromTime / Nubmer, toTime / Number
return : this / SceneItem

addTimingFunction
timingFunction을 추가한다.
arguments: fromTime / Number, toTime / Number. curveArray / Array
return : this
example : sceneItem.addTimingFunction(0, 1, [0,0,1,1]); // 0s ~ 1s cubic-bezier([0,0,1,1])로 설정

Frame
getProperty
property에 해당하는 값을 가져온다.
arguments : property / String
return : value / String | Number | Object

getTransform
property에 해당하는 transform 값을 가져온다.
arguments : property / String
return : value / String | Number | Object

getFilter
property에 해당하는 transform 값을 가져온다.
arguments : property / String
return : value / String | Number | Object

getCSSObject
property, transform, filter를 CSSObject형식으로 가져온다.
return : CSSObject / Object

getCSSText
property, transform, filter를 CSSText 형식으로 가져온다.
return : CSSText / String

copy
프레임을 복사한다.
return : frame / Frame:Object

merge
다른 프레임과 병합을 한다.
arguments : frame / Frame:Object
return : this / Frame:Object
